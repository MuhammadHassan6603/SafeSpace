import 'package:flutter/material.dart';

class GmailScreen extends StatelessWidget {
  const GmailScreen({super.key});

  @override
  Widget build(context) {
    return Center(
      child: Text('Gmail Screen'),
    );
  }
}