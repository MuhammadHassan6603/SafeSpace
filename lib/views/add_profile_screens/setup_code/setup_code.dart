import 'package:flutter/material.dart';

class SetupCodeScreen extends StatelessWidget {
  const SetupCodeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Setup Code'),
    );
  }
}